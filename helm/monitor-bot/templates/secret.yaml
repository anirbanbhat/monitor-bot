apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
stringData:
  # The token is taken from values.yaml, which should be set during 'helm install'
  telegram-token: {{ .Values.telegramToken | quote }}